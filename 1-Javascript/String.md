#### String to Array

```
let str = "New York";
let arr = str.split("");
let arr = Array.from(str);
```

#### Method names common for Strings and Arrays

-   concat
-   slice
-   for loop

```
for (let chr of str) {

}
```
